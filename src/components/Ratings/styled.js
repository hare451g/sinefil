import styled from 'styled-components';

const RatingContainer = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-between;
`;

const Heading = styled.div`
  font-style: normal;
  font-weight: bold;
  font-size: 24px;
  line-height: 28px;
  padding-bottom: 12px;
`;

export { Heading, RatingContainer };
