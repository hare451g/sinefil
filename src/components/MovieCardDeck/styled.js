import styled from 'styled-components';

const Deck = styled.div`
  max-width: 720px;
  flex-direction: row;
  display: flex;
  flex-wrap: wrap;
`;

const CardWrapper = styled.div`
  margin: 4px 6px;
`;

export { Deck, CardWrapper };
